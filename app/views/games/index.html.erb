<h1>Games</h1>

<%  @games.each do |game| %>
    <h2><%= link_to game.name, game_path(game) %></h2>
    <ul>
      <li>Developer: <%= game.developer %></li>
      <li>Genre: <%= game.genre %></li>
    </ul>

    <% end %>
