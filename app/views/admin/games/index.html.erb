<h1>Master List of Games</h1>
  <ul>
    <% @games.each do |game| %>
      <li><%= link_to game.name, admin_game_path(game) %></li>
    <% end %>
  </ul>

<%= link_to 'Back to Control Panel', admin_home_index_path %>
